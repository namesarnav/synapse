global: { scrape_interval: 5s }
scrape_configs:
  - job_name: api
    static_configs: [{ targets: ["api:8080"] }]
  - job_name: workers
    dns_sd_configs: [{ names: ["worker"], type: A, port: 8081 }]
  - job_name: scheduler
    static_configs: [{ targets: ["scheduler:8081"] }]
